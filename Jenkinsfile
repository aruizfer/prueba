pipeline { 
    agent any 
        stages { 
            stage ('Build') { 
                steps { 
                    echo 'Running build phase. Go to checkout scm'
                    checkout scm
                    echo 'checkout scm DONE'
                }
            }
        }
    }
